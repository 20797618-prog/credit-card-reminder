# 信用卡还款提醒器 · 产品需求文档（PRD）

> 版本：V1（2026-09）
> 状态：待评审

---

## 1. 背景与目标

持卡人往往有多张信用卡，各卡账单日、还款日不同，容易漏还导致逾期、影响征信。

本工具解决的核心问题：**登录后一目了然地看到每张卡距离还款日还剩多少天，临近时醒目预警，提醒用户不要忘记还款。**

定位是一个「还款倒计时仪表盘」，而非一次性弹通知的闹钟。

## 2. 目标用户与场景

- **用户**：个人持卡人，1 张到多张信用卡。
- **核心场景**：
  1. 录入多张信用卡（仅卡号后 4 位 + 还款日）。
  2. 每天打开页面，看到所有卡的剩余还款天数，最紧急的排最前、颜色最醒目。
  3. 还款后点「本期已还」，该卡回到「已还」绿色状态，倒计时自动滚到下一期。
- **多用户**：复用现有账号体系，不同邮箱登录看到各自独立的卡片数据。

## 3. 范围

### 3.1 本期（V1）实现

- 复用账号体系：邮箱验证码登录 / 邮箱密码登录。
- 信用卡增、删、改、查（仅存卡号后 4 位）。
- 还款日倒计时与分级预警（正常 / 临近 / 紧急 / 逾期）。
- 「本期已还」标记，自动滚动到下一期，并记录每次还款历史。
- 还款记录查看：按年 / 月 / 日三级筛选历史还款记录。
- 用户数据隔离（不同邮箱数据互不可见）。
- 响应式页面，手机浏览器可用。

### 3.2 本期不做（预留，后续版本）

- 还款金额的自动读取（通过邮箱账单解析）——本期仅预留字段与接口。
- 邮件 / 短信 / 手机推送提醒（用户已确认 V1 不做定时任务）。
- 完整卡号的补录（本期只存后 4 位，字段结构已为补全预留）。
- 账单日提醒（账单日本期仅作可选记录，不做提醒逻辑）。

## 4. 功能需求

### 4.1 登录（复用账号体系）

- 两种方式：邮箱 + 验证码、邮箱 + 6 位数字密码。
- 未注册邮箱首次登录自动建号。
- 会话 30 天有效，`Authorization: Bearer <token>` 传递。
- 前端复用 `auth.js` 的 `AccountAuth`。

### 4.2 卡片管理

- **新增卡片**：填写卡别名（必填）、卡号后 4 位（必填，4 位数字）、银行（可选）、账单日（可选，1–31）、还款日（必填，1–31）。
- **编辑卡片**：可修改以上任意字段。
- **删除卡片**：二次确认后删除。
- **卡片列表**：展示所有卡片及其还款状态。

### 4.3 还款倒计时与预警

每张卡实时计算并显示「距离还款日还剩 N 天」或「已逾期 N 天」，按紧迫度分级着色：

| 状态 | 判定 | 显示 | 颜色 |
| --- | --- | --- | --- |
| 正常 | 剩余 > 7 天 | 剩 N 天 | 常规 |
| 临近 | 剩余 3–7 天（含 7） | 剩 N 天 | 黄 |
| 紧急 | 剩余 0–2 天（含当天） | 剩 N 天 / 今天到期 | 橙 |
| 逾期 | 已过还款日未还 | 已逾期 N 天 | 红 |
| 已还 | 本期已标记还款 | 已还 · 下期 N 天后 | 绿 |

列表默认按「剩余天数升序」排序，最紧急的置顶。

### 4.4 本期已还标记

- 每张卡一个「本期已还」按钮（始终显示），点击后：
  - 该卡状态变为「已还」，预警消失。
  - 倒计时自动指向下一期还款日。
  - 往该卡追加一条还款历史记录（应还日 + 实际还款日）。
- 按钮字体颜色与「编辑 / 删除」等次要按钮一致（灰），不做绿色强调。

### 4.5 还款记录查看

- 主页面顶部有「我的信用卡 / 还款记录」两个 Tab 切换。
- 还款记录页列出历史还款记录，每条展示：卡别名、尾号、**应还日**（年 / 月 / 日）、**还款日**（实际还款的年 / 月 / 日）。
- 支持按 **年、月、日** 三个维度筛选（下拉联动，均含「全部」选项）。
- 记录按还款时间倒序排列。

### 4.6 数据隔离

- 卡片数据按登录邮箱隔离，存储在独立 KV。
- 任何接口必须先通过会话校验，拿不到登录身份一律返回 401。

## 5. 数据模型

存储于独立业务 KV（`CARDS_KV`）。以「每用户一张列表」的方式存储，键为 `cards:<email>`，值为 JSON 数组。

```jsonc
// cards:<email> 的值
[
  {
    "id": "uuid",            // 卡唯一标识（前端生成或后端生成）
    "alias": "招行主卡",      // 卡别名（必填）
    "last4": "1234",          // 卡号后 4 位（必填，仅存 4 位）
    "bank": "招商银行",       // 银行（可选）
    "billDay": 10,            // 账单日（可选，1–31）
    "payDay": 20,             // 还款日（必填，1–31）
    "amount": null,           // 本期应还金额（预留，V2 由邮箱账单回填）
    "amountSource": null,     // 金额来源（预留：manual | email）
    "lastPaidDue": "2026-09-20", // 最后已还的那个还款日（ISO 日期）；新建默认「最近一期已还」
    "paidHistory": [              // 还款历史记录（按还款时间追加）
      { "dueDate": "2026-09-20", "paidAt": "2026-09-16" }  // 应还日 / 实际还款日
    ],
    "createdAt": 1750000000000,
    "updatedAt": 1750000000000
  }
]
```

字段说明：
- `last4`：只存 4 位数字，不存完整卡号，从源头规避敏感信息。
- `amount` / `amountSource`：为 V2「邮箱账单读取金额」预留，V1 恒为 `null`。
- `lastPaidDue`：驱动「本期已还」状态机的唯一字段，见 §6。
- `paidHistory`：还款历史数组，每条记录 `dueDate`（本期应还日）与 `paidAt`（实际还款日），供 §4.5 还款记录页聚合展示；删除卡片时随卡一并删除。

## 6. 核心算法（还款日计算）

还款日为「每月固定日」，需处理跨月与月末边界。

### 6.1 月末边界

某年某月的还款日 = `min(payDay, 该月天数)`。例如还款日 31 号，在 2 月则取 2 月最后一天（28/29）。

### 6.2 关键日期函数

设 `payDay` 为还款日，`today` 为今天：

- `dueOfMonth(payDay, y, m)`：返回 y 年 m 月的还款日（含月末边界）。
- `currentDue(payDay, today)`：最近一次「到期日」。
  - 本月还款日 ≤ 今天 → 本月还款日；
  - 否则 → 上月还款日。
- `nextDueAfter(due)`：到期日 `due` 的下一个还款日（下月对应日）。

### 6.3 状态机

| 条件 | 状态 | 关键数值 |
| --- | --- | --- |
| `lastPaidDue == currentDue` | 已还 | 下期应还日 = `nextDueAfter(currentDue)`，剩余 = 下期 - 今天 |
| 否则（未还）且 `currentDue ≥ today` | 未还 | 剩余 = `currentDue - today`（0 = 今天到期） |
| 否则（未还）且 `currentDue < today` | 逾期 | 逾期天数 = `today - currentDue` |

标记「已还」时，后端计算 `currentDue` 写入 `lastPaidDue`。月份推进后 `currentDue` 自然更新，`lastPaidDue` 不匹配即自动回到未还，无需额外定时任务。

> 语义约定：工具每张卡同时只追踪「最近一期」。连续多期逾期时，点一次「已还」推进一期。

## 7. 技术方案

沿用账号体系的 **Cloudflare Pages + Functions + KV**。

- **部署**：Cloudflare Pages，静态前端 + Pages Functions 后端。
- **账号**：绑定中央 `ACCOUNTS_KV`（id `e8d41d4739e94a80b3f566fc0d549e96`），复用登录与会话。
- **业务数据**：独立 `CARDS_KV`，存储卡片列表。
- **鉴权**：业务接口调用 `_account_lib.js` 的 `resolveSession(request, env)` 拿 `email`。
- **前端**：原生 HTML + CSS + JS（无框架），引入 `auth.js` 复用登录。

项目结构：

```
信用卡还款提醒器/
├── README.md
├── PRD.md
├── wrangler.jsonc            # KV 绑定（ACCOUNTS_KV + CARDS_KV）
├── index.html                # 单页应用（登录 + 卡片列表）
├── style.css
├── app.js                    # 业务前端逻辑
├── auth.js                   # 复制自 account-system/frontend/
└── functions/
    ├── _account_lib.js       # 复制自 account-system/backend/
    ├── _mail.js              # 复制自 account-system/backend/
    ├── _cards_lib.js         # 新增：卡片业务库（还款日算法 + 数据读写）
    └── api/
        ├── auth/             # 复制整套（request-code / verify-code / login-password / set-password / me）
        └── cards/
            ├── index.js      # GET 列表 / POST 新增
            ├── [id].js       # PUT 编辑 / DELETE 删除
            └── paid.js       # POST 标记本期已还
        └── repayments.js      # GET 还款记录聚合
```

## 8. 接口设计

> 业务接口均需登录态（`Authorization: Bearer <token>`），否则 401。

| 方法 | 路径 | 说明 | 请求体 |
| --- | --- | --- | --- |
| GET | `/api/cards` | 获取当前用户全部卡片 | — |
| POST | `/api/cards` | 新增卡片 | `{alias, last4, bank?, billDay?, payDay}` |
| PUT | `/api/cards/:id` | 编辑卡片 | 同上（部分字段） |
| DELETE | `/api/cards/:id` | 删除卡片 | — |
| POST | `/api/cards/:id/paid` | 标记本期已还（推进指针并追加还款记录） | — |
| GET | `/api/repayments` | 获取当前用户全部还款记录（跨卡聚合，倒序） | — |

响应统一 `{ ok: true, ... }` 或 `{ ok: false, error: "..." }`。列表接口返回每张卡的**计算后字段**：`daysLeft`（剩余天数，负数为逾期）与 `status`（normal / warning / urgent / overdue / paid）。

## 9. 页面与交互

- **登录页**：邮箱 + 验证码 / 密码双 Tab，登录成功进入主页面。
- **主页面**：
  - 顶部：登录邮箱 + 退出按钮。
  - 顶部 Tab：「我的信用卡 / 还款记录」。
  - 卡片列表：按剩余天数升序；每张卡显示别名、后 4 位、银行、还款日、本期年月、「剩 N 天」倒计时 + 状态色、「本期已还」按钮、「编辑」「删除」入口。
  - 还款记录页：年 / 月 / 日三级筛选 + 历史还款记录列表。
  - 「添加卡片」按钮 + 表单弹层。
- **空状态**：无卡片时展示引导文案与添加入口。
- 移动端响应式，卡片纵向堆叠。

## 10. 非功能需求

- **安全**：卡号仅存后 4 位；业务接口强制会话校验；不存完整卡号。
- **时区**：还款日计算统一按服务器时区（UTC）取「今天」，避免本地时钟差异；前端展示按本地时区。
- **数据量**：单用户卡片量小（< 100 张），KV 单条列表读取即可，无需分页。
- **可靠性**：复用账号体系已验证的会话 / 验证码 / 限流逻辑。

## 11. 未来扩展（V2 预留）

- **邮箱账单读取金额**：抽象 `BillReader` 接口（V1 空实现），V2 通过 IMAP 拉取银行账单邮件 → 解析应还金额 → 回填 `amount` 字段，`amountSource` 标记为 `email`。
- **补全卡号**：数据模型已含 `bank` 等字段，可扩展完整卡号（需加密存储）。
- **推送提醒**：可加 Cloudflare Cron 定时任务 + 邮件推送（复用现有 `_mail.js`）。

## 12. 开发计划

| 阶段 | 内容 | 产出 |
| --- | --- | --- |
| M1 | 项目骨架 + 复制账号体系 + KV 绑定 | 可登录的空项目 |
| M2 | 后端卡片业务库 + CRUD API + 还款日算法 | 全部接口可用 |
| M3 | 前端登录 + 卡片列表 + 倒计时 + 增删改 + 已还标记 | 完整可用页面 |
| M4 | 样式打磨 + 响应式 + 联调 + README 部署说明 | 可部署交付 |

---

*本 PRD 已与用户确认的关键决策：提醒方式=网页内倒计时+预警；还款状态=有「本期已还」标记；还款日=固定每月几号；技术栈=Cloudflare Workers + KV。*
