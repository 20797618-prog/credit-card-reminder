# Cloudflare wrangler 本地缓存（含本地 KV 模拟数据）
.wrangler/

# Node 依赖（如有）
node_modules/

# 系统文件
.DS_Store

# 项目记忆与本地元数据（不入库）
.workbuddy/

# 本地开发 secrets（含 SMTP 授权码等，绝不入库）
.dev.vars
.dev.vars.*
# 本地回归脚本（含本机绝对路径，不入库）
verify-layout.mjs
# 本地专用脚本（含本机绝对路径，不入库）
verify-pwa.mjs
gen-icons.mjs
verify-i18n.mjs
verify-overflow.mjs
shot-i18n.mjs
