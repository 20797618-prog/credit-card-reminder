# 邮箱账单导入 · 方案文档（V1：中信银行 + 方案B转发 + 手动触发）

> 版本：V1（2026-09-17）
> 状态：**待用户确认，未开发**

---

## 1. 目标

登录后点一下「导入账单」，工具从中信银行账单邮件里读出**本期应还金额**，自动填到对应的卡上。用户不用手抄金额。

**本方案确定的三条边界（用户已拍板）**：
1. **方案 B（转发）**：用户在自己邮箱设一条转发规则，账单邮件转发到工具专用收件邮箱。**用户零授权**——不交密码、不交授权码。
2. **手动触发**：用户点「导入账单」才拉取解析，无需定时任务。
3. **先做中信银行**一家，解析规则表驱动，后续加银行 = 加一条规则。

---

## 2. 安全承诺（必须展示给用户的文案）

这部分是你特别要求的「明确告诉用户」的内容，写进前端的说明弹窗里，一字不改：

> ### 🔒 关于你的邮箱安全
>
> **我们永远不会要求你提供邮箱授权码或密码。**
>
> 有些工具会要求你交出「IMAP 授权码」。你要知道那意味着什么：
> 那串 16 位授权码等于你邮箱的**完全控制权**——拿到它可以
> **读取你的所有邮件、删除邮件、甚至以你的名义发邮件**。
> 任何索要全授权码的工具，都是在让你承担这个风险。我们不这么做。
>
> **我们只接收你主动转发过来的账单邮件。** 你在自己邮箱里设置一条
> 转发规则（2 分钟设置一次，长期有效），之后你的邮箱对我们来说是
> 完全封闭的——我们既读不到、也删不了、更发不出你邮箱里的任何东西。
>
> 专属收件邮箱只存账单邮件，解析完成后只保留「金额 + 卡尾号」两个
> 字段，邮件原文不落库。

---

## 3. 架构与数据流

```
┌─────────────┐   转发规则(发件人含中信→转发)   ┌──────────────────┐
│ 用户 QQ 邮箱  │ ──────────────────────────→ │ 工具专用收件邮箱    │
└─────────────┘      用户自己在邮箱设置，零授权      │ (tools@qq.com)   │
                                                └────────┬─────────┘
                                                         │ 用户点「导入账单」时
                                                         │ Worker 用 IMAP 拉取(只读)
                                                         ▼
                                                ┌──────────────────┐
                                                │ 解析内核(中信规则)  │
                                                │ → 金额/最低还款/    │
                                                │   到期日/卡尾号     │
                                                └────────┬─────────┘
                                                         │ 按尾号匹配用户卡
                                          ┌──────────────┴──────────────┐
                                          ▼                             ▼
                                    匹配成功 → 回填金额            匹配失败 → 待确认列表
                                    (amount + 应还日)             (用户手动指定归属卡)
```

**收件邮箱实现选型**（方案 B 内部的两条路，需你确认走哪条）：

| | B2：QQ 专用邮箱 + IMAP 拉取（推荐起步） | B1：Cloudflare Email Routing（后续升级） |
|---|---|---|
| 原理 | 转发到某个 QQ 邮箱，点「导入」时 Worker 用 IMAP 只读拉取 | 用户转发到 `bill@你的域名`，邮件到达即触发 Email Worker 解析 |
| 前提 | 无（授权码现成，就是现在发验证码那个） | 需要一个托管在 Cloudflare 的自有域名 |
| 触发 | 手动（贴合你的决定） | 邮件到达即解析，「导入」按钮退化成刷新 |
| 风险 | QQ 的 IMAP 在 Workers 里需实测（见 §7 技术风险） | 无 IMAP 依赖，最干净 |

**推荐**：先做 B2 跑通全链路（不依赖域名），如果你有域名再升级 B1，解析内核两个通用。

---

## 4. 中信账单解析规则（基于你的截图）

从截图提取到中信账单邮件的固定字段结构：

| 字段 | 邮件原文 | 截图样本值 | 提取正则思路 |
|---|---|---|---|
| 本期应还总额 | `本期应还总额 Total Payment` | CNY 3,038.44 | `本期应还总额` 后第一个 `CNY\s*[\d,]+\.\d{2}`（注意千分位逗号） |
| 本期最低还款额 | `本期最低还款额 Min. Payment` | CNY 303.62 | 同上，锚点换 `最低还款额` |
| 到期还款日 | `到期还款日 Payment Due Date` | 2026年09月10日 | `到期还款日` 后第一个 `\d{4}年\d{2}月\d{2}日` |
| 账单日 | `账单日 Bill Date` | 2026年08月23日 | 同上，锚点换 `账单日` |
| 卡尾号 | **截图中被遮住，需确认** | ？ | 待你确认邮件里是否显示「尾号 XXXX」（见 §8 问题1） |

**邮件识别**：发件人含 `citic` 或 `citiccard`，或主题含「中信银行信用卡」+「账单」。

**解析产出**：`{ last4, amount, minPayment, dueDate, billDate }`。

---

## 5. 数据模型扩展（每张卡新增字段）

```
"amount":        3038.44,      // 本期应还金额（启用之前预留的字段）
"minPayment":    303.62,       // 本期最低还款额（新增）
"amountDueDate": "2026-09-10", // 该金额对应的到期还款日（关键：金额是有有效期的，
                               //  换期后金额不适用，倒计时以 payDay 为准，金额以这个日期匹配本期）
"amountSource":  "citic-email",// 金额来源标识（为后续其他银行/手动录入预留）
"amountUpdatedAt": 1758000000000
```

**金额与期的匹配逻辑**：解析出的 `dueDate` 与该卡当前应还日所在月份一致 → 直接回填；不一致（比如导入的是上期账单）→ 不回填，提示「账单不是本期的」。

---

## 6. UI 与交互

### 6.1 入口

「我的信用卡」工具栏加一个「📥 导入账单」按钮（在「＋ 添加卡片」旁边）。

### 6.2 首次点击 → 说明弹窗（含 §2 安全承诺全文）

弹窗结构：
1. 安全承诺全文（§2，可展开收起，默认展开）。
2. 「它有什么用」：自动把中信账单的本期应还金额填到对应卡上，不用手抄。
3. 「怎么设置（一次性，约 2 分钟）」——QQ 邮箱操作指引：
   - 登录 QQ 邮箱网页版 → 设置 → 「收信规则」→ 创建规则；
   - 条件：发件人包含 `citic`（或「中信银行」）；
   - 动作：**转发到** `工具专用收件邮箱`（弹窗里直接展示邮箱地址，一键复制）；
   - 保存。之后每期账单邮件会自动转过来。
4. 按钮：「我设置好了，去导入」→ 关闭弹窗并执行导入。

### 6.3 导入过程与结果反馈

- 点击导入 → 按钮转菊花「正在拉取账单…」。
- **成功**：toast「已导入 N 张卡的账单金额」，卡片列表上对应卡直接显示「本期应还 ¥3,038.44」。
- **解析到但匹配不上卡**（尾号对不上你录入的任何一张）：进「待确认」区——列表显示「中信账单 ¥3,038.44 · 尾号 1234 · 到期 09-10」，下拉选择归属到哪张卡，或忽略。
- **没有新账单**：toast「未发现新账单邮件」。
- **失败**：toast 显示原因（如邮箱连接失败），不中断页面。

### 6.4 卡片上的金额展示

- 方案 A/B 卡片上，「本期 X年X月」旁边加「应还 ¥3,038.44」（金额存在时）。
- 金额旁一个小 ⓘ，悬浮/点击显示来源：「来自中信账单邮件 · 导入于 09-17 21:50」+ 最低还款额。

---

## 7. 后端接口与技术风险

### 7.1 新增接口

| 方法 | 路径 | 功能 |
|---|---|---|
| POST | `/api/bills/import` | 拉取收件邮箱 → 解析 → 回填/进待确认。返回 `{imported, pending[], skipped}` |
| GET | `/api/bills/pending` | 待确认列表 |
| POST | `/api/bills/confirm` | 用户指定归属卡后回填 |
| DELETE | `/api/bills/pending/:id` | 忽略某条待确认 |

待确认记录按登录用户隔离存 KV（独立 key：`bills_pending:<email>`）。

### 7.2 解析内核（`functions/_bill_parser.js`）

表驱动：
```js
const BANK_RULES = [
  {
    bank: 'citic',
    match: (from, subject) => /citic/i.test(from) || /中信银行.*信用卡/.test(subject),
    parse: parseCitic   // 返回 { last4, amount, minPayment, dueDate, billDate } 或 null
  }
];
```
后续加银行 = 加一条规则，不动其他代码。

### 7.3 技术风险（诚实列出）

| 风险 | 说明 | 对策 |
|---|---|---|
| **QQ IMAP in Workers 未实测** | `cf-imap` 库基于 `cloudflare:sockets`，QQ IMAP（imap.qq.com:993）有特殊性（要求 ID 命令等） | 开工第一步先做连通性 spike（半小时级）：真实连一次、LIST、拉一封。**连不通则整个 B2 换道 B1（需域名）或 C（粘贴）**，这也是先 spike 再写业务的原因 |
| 转发邮件格式失真 | QQ 转发默认保留原文，但部分客户端会重排 HTML | 解析失败时把该邮件标记「解析失败」进待确认区，附原始文本片段让用户核对 |
| 金额千分位 | `3,038.44` | 正则统一处理逗号 |
| 一封邮件多卡 | 中信一封账单一般一张卡；若出现多卡按尾号逐个匹配 | 按解析出的尾号数组逐条产出 |

### 7.4 专用收件邮箱

默认复用现有 `527388531@qq.com`（发验证码用 SMTP 发信，收账单用 IMAP 读信，同邮箱不冲突，授权码同一个，零新增配置）。
若你想要隔离，注册一个新的专用 QQ 邮箱告诉我，我替换配置（1 分钟的事）。

---

## 8. 需要你确认的问题

1. **中信账单邮件里有没有卡尾号？** 截图里卡号被打了码看不到。请翻一封原始账单邮件确认（一般是「尾号 XXXX」或「卡末四位」字样）。
   - 有的话，匹配卡最稳。
   - 没有的话，降级策略：解析出的金额进待确认区，由用户手动归属到卡（多一步点击，可用性稍降）。
2. **收件邮箱**：直接用现有 `527388531@qq.com`，还是新注册一个专用邮箱？
3. **B2 起步** 是否 OK（先 QQ IMAP，跑通后如有域名再升级 B1）？
4. **最低还款额**要不要也展示？（默认：展示在金额详情里，主显示本期应还总额）

---

## 9. 确认后的开发顺序（预计）

1. IMAP 连通性 spike（风险前置，见 §7.3）→ 决定走 B2 还是换道。
2. 解析内核 `parseCitic` + 单元测试（用截图样本 + 变体：无尾号/千分位/非本期）。
3. 后端 4 个接口 + KV 待确认存储。
4. 前端：入口 + 安全承诺弹窗 + 设置指引 + 导入流程 + 待确认区 + 卡片金额展示。
5. 端到端验证（puppeteer 走真实流程）+ 部署上线。

---

*本文档仅为方案，未写任何业务代码。*
